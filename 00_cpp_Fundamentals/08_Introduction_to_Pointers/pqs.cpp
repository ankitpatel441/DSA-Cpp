#include <iostream>
using namespace std;

// Qs. What will ptr2 point to in the following code:
// int x = 5, y = 10;
// int *ptr1 = &x, *ptr2 = &y;
// ptr2 = ptr1;

// Qs. What is the type of a & b :
// float* a, b;
// a - float ptr
// b - normal  float

//Qs. Can we initialize a pointer with value 0 ? 

int main() {
// int x = 5, y = 10;
// int *ptr1 = &x, *ptr2 = &y;
// ptr2 = ptr1;

// cout << ptr2 << endl;
// cout << ptr1 << endl;
// cout << &x << endl;

// float* a, b;

// int *ptr = 0;
// cout << ptr << endl;


    return 0;
}